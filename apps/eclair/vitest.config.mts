// Default vitest config for eclair - uses browser mode
// This file is picked up by NX's vitest plugin for the "test" target
import browserConfig from './vitest.browser.config.mts'

export default browserConfig
