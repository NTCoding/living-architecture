# Flows

Shows entry points (UI routes, APIs, scheduled jobs) and traces execution paths through components.

## Key Actions

- **Filter by entry type** — Show only UI, API, or scheduled job entry points
- **Expand a flow card** — See the full execution trace from entry point through domain operations and events
- **Filter by domain** — Show flows from specific domains
