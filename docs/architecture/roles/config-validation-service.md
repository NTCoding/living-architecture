# config-validation-service

`config-validation-service` validates extraction-config data and formats validation results.

- Keep it in `packages/riviere-extract-config/src/` validation code.
- Use it for schema checks, semantic validation, and validation-result mapping.
- Do not mix config persistence or extraction execution into this role.
