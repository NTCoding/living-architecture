# glob-matcher

`glob-matcher` wraps deterministic glob-pattern matching infrastructure.

- Keep it in `packages/riviere-extract-ts/src/platform/infra/glob-matching/`.
- Use it for path-pattern matching adapters only.
- Do not blend extraction-domain policy into this role.
