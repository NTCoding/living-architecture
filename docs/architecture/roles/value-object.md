# value-object

A value object models an immutable concept defined entirely by its value.

- Keep value objects side-effect free and immutable after creation.
- Centralize parsing and normalization rules so equality remains consistent.
