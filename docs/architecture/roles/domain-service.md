# domain-service

A domain service contains domain behavior that does not naturally belong to a single entity or aggregate.

- Keep business rules explicit and deterministic.
- Do not mix transport, persistence, or framework orchestration concerns into domain services.
