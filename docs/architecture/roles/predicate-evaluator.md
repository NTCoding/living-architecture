# predicate-evaluator

`predicate-evaluator` decides whether source elements satisfy extraction predicates.

- Keep it in `packages/riviere-extract-ts/src/features/extraction/domain/predicate-evaluation/`.
- Use it for deterministic predicate logic only.
- Do not own extraction orchestration or config loading here.
