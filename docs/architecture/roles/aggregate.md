# aggregate

An aggregate is the consistency boundary that exposes behavior through a small public API.

- Keep invariants and state transitions inside the aggregate boundary.
- Do not bypass aggregate behavior with external mutation helpers.
