# entity

An entity is a domain object with stable identity that persists across state changes.

- Preserve identity semantics through lifecycle operations.
- Avoid modeling entities as interchangeable by attribute values alone.
