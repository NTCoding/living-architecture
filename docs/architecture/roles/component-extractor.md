# component-extractor

`component-extractor` extracts draft components from TypeScript source files.

- Keep it in `packages/riviere-extract-ts/src/features/extraction/domain/component-extraction/`.
- Use it for file-level extraction, module matching, and creation of draft component records.
- Do not detect inter-component connections in this role.
