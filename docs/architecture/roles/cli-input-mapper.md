# cli-input-mapper

`cli-input-mapper` converts CLI-facing strings and flags into validated internal input data.

- Keep it under `infra/cli/input/`.
- Use it for parsing, validation, normalization, and input mapping.
- Do not print output, exit the process, or orchestrate use cases here.
