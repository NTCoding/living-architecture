# source-file-filter

`source-file-filter` narrows source-file sets before later extraction or processing.

- Keep it in source-filtering infrastructure.
- Use it for deterministic path filtering and inclusion or exclusion logic.
- Do not load graphs, parse CLI flags, or perform extraction itself here.
