# cli-result-presenter

`cli-result-presenter` turns command results into CLI-facing output.

- Keep it focused on formatting selection and stdout or stderr presentation.
- Use dedicated helpers for low-level formatting details when needed.
- Do not orchestrate extraction or query execution here.
