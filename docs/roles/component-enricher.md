# component-enricher

`component-enricher` adds extracted values onto previously detected draft components.

- Keep it in `packages/riviere-extract-ts/src/features/extraction/domain/value-extraction/enrich-components.ts`.
- Use it for enrichment orchestration that stays inside extraction-domain logic.
- Do not treat it as a generic formatter or config loader.
