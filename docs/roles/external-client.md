# external-client

`external-client` wraps deterministic access to an external tool or service.

- Keep it in infrastructure client code such as git, GitHub, Nx, or extraction project loading.
- Use it for process execution, network calls, and external-system adapters.
- Do not hide project-specific workflow policy in this role.
