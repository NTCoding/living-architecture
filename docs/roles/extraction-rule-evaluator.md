# extraction-rule-evaluator

`extraction-rule-evaluator` applies value-extraction rules to already selected components.

- Keep it in `packages/riviere-extract-ts/src/features/extraction/domain/value-extraction/` rule-evaluation files.
- Use it for rule interpretation, value extraction, and rule-specific traversal helpers.
- Do not choose modules or detect graph connections in this role.
