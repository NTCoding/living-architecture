# path-pattern-matcher

`path-pattern-matcher` provides deterministic path-normalization and pattern-matching helpers.

- Keep it in `packages/riviere-role-enforcement/src/platform/infra/path-patterns.ts`.
- Use it for reusable path matching infrastructure only.
- Do not couple it to repository-specific role names.
