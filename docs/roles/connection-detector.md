# connection-detector

`connection-detector` finds architectural connections from extracted source information.

- Keep it in `packages/riviere-extract-ts/src/features/extraction/domain/connection-detection/`.
- Use it for configurable detection, async detection, call-graph building, and supporting lookup structures.
- Keep external adapters separate from this role.
