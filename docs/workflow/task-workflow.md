# Task Workflow

Canonical workflow guidance lives in `tools/dev-workflow-v2/README.md`.

Use the command docs under `tools/dev-workflow-v2/commands/` for the exact command flow.

## Starting work

Use `tools/dev-workflow-v2/README.md` for the task selection and implementation entry flow.
