{
  "name": "riviere-role-enforcement",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --build tsconfig.lib.json && pnpm exec esbuild src/features/check/infra/oxlint-plugin.ts --bundle --platform=node --format=cjs --outfile=dist/features/check/infra/oxlint-plugin.cjs",
        "cwd": "{projectRoot}"
      }
    }
  }
}
