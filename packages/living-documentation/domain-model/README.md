# domain-model

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build @living-architecture/living-documentation-domain-model` to build the library.

## Running unit tests

Run `nx test @living-architecture/living-documentation-domain-model` to execute the unit tests via [Vitest](https://vitest.dev/).
