---
name: dev-workflow-start-planning
description: Start or resume a local product-discovery planning topic in this repository. Use only when the user explicitly invokes $dev-workflow-start-planning with a topic or planning ID.
---

# Start Planning

Use the supplied topic or planning ID. Follow the canonical procedure in `tools/dev-workflow-v2/commands/start-planning.md` exactly.

When it directs the user to the next Claude command, report the Codex equivalent: `$dev-workflow-continue-planning`.
