---
name: dev-workflow-start-planning
description: Start or resume a product-discovery planning topic. Use only when the user explicitly invokes $dev-workflow-start-planning with a topic or planning ID.
---

# Start Planning

Use the supplied topic or planning ID. Follow the canonical procedure in the plugin's `commands/start-planning.md` exactly.

When it directs the user to the next Claude command, report the Codex equivalent: `$dev-workflow-continue-planning`.
