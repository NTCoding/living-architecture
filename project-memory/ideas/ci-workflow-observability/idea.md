# CI Workflow Observability

**Status:** Captured

**Need level:** Probably needed

**Source:** `docs/project/PRD/riviere-extraction-workflows-v1/PRD.md` and planning discussion for `riviere-extraction-workflows-v1`

**Reason deferred:** V1 should set up the `ecommerce-demo-app` workflow in CI as a normal CLI command, but no CI-specific behaviour is required yet.

**Priority signal:** User said to see what happens when the workflow is used in CI and evolve the CI observability strategy from real feedback; no ordering priority confirmed.

**Dependencies:** Real feedback from running the `ecommerce-demo-app` workflow in CI.

---

## Summary

Evolve CI-specific workflow observability after Rivière has real feedback from running the `ecommerce-demo-app` workflow in CI.

## Why this may matter

The user wants CI observability strategy to evolve from real usage rather than being designed speculatively in V1.

## Open questions

- How should CI observability evolve after Rivière has real feedback from running the `ecommerce-demo-app` workflow in CI?

## Links

- `docs/project/PRD/riviere-extraction-workflows-v1/PRD.md`
- `docs/project/PRD/riviere-extraction-workflows-v1/deferred-items.md`
